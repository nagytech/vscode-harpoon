/**
 * Process state signifies if the process has started or was
 * terminated
 *
 * @export
 * @enum {number}
 */
export enum ProcessState {
    /**
     * Process was started (0)
     */
    STARTED = 0,
    /**
     * Process was terminated (1)
     */
    TERMINATED = 1
}
